# Dental demo SPA - serve index.html for client-side routes
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /demo/dental-clinic/
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]
  RewriteRule ^ index.html [L]
</IfModule>
