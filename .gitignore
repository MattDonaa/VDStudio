# Dependencies & build artefacts (demos are committed as built output)
node_modules/
dist/
out/
.next/
.cache/

# Environment & secrets (Supabase anon keys are inlined in the built demos,
# which is safe by design; raw .env files are not committed)
.env
.env.*
!.env.example

# Editor / OS / tooling
.claude/
.DS_Store
Thumbs.db
*.log
